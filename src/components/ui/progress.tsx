import { StyleSheet, View } from 'react-native';
import { ThemedText } from '@/components/ui/text';
import { colors, radii, spacing } from '@/components/ui/theme';
export function Progress({ value, label, showValue = true }: { value: number; label?: string; showValue?: boolean }) { const safeValue = Math.max(0, Math.min(100, Math.round(value))); return <View accessible accessibilityRole="progressbar" accessibilityValue={{ min: 0, max: 100, now: safeValue }} style={styles.container}>{label || showValue ? <View style={styles.row}>{label ? <ThemedText type="small">{label}</ThemedText> : <View />}{showValue ? <ThemedText type="small" style={styles.value}>{safeValue}%</ThemedText> : null}</View> : null}<View style={styles.track}><View style={[styles.bar, { width: `${safeValue}%` }]} /></View></View>; }
const styles = StyleSheet.create({ container: { gap: spacing.sm }, row: { flexDirection: 'row', justifyContent: 'space-between', gap: spacing.md }, value: { fontWeight: '700', color: colors.text }, track: { height: 8, backgroundColor: colors.surfaceMuted, borderRadius: radii.sm, overflow: 'hidden' }, bar: { height: '100%', backgroundColor: colors.primary, borderRadius: radii.sm } });
