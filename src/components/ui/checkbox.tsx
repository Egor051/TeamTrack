import { Pressable, StyleSheet, View } from 'react-native';
import { ThemedText } from '@/components/ui/text';
import { radii } from '@/components/ui/theme';
import { useTheme } from './theme-provider';
export function Checkbox({ checked, disabled, label, onPress }: { checked: boolean; disabled?: boolean; label: string; onPress: () => void }) { const { colors: theme } = useTheme(); return <Pressable accessibilityRole="checkbox" accessibilityLabel={label} accessibilityState={{ checked, disabled }} disabled={disabled} onPress={onPress} hitSlop={6} style={({ pressed }) => [styles.touch, pressed && styles.pressed]}><View style={[styles.box, { borderColor: theme.borderStrong, backgroundColor: theme.surface }, checked && { backgroundColor: theme.primary, borderColor: theme.primary }, disabled && styles.disabled]}>{checked ? <ThemedText style={[styles.mark, { color: theme.surface }]}>✓</ThemedText> : null}</View></Pressable>; }
const styles = StyleSheet.create({ touch: { width: 48, height: 48, alignItems: 'center', justifyContent: 'center' }, box: { width: 26, height: 26, borderRadius: radii.sm, borderWidth: 2, alignItems: 'center', justifyContent: 'center' }, mark: { fontWeight: '800', lineHeight: 20 }, disabled: { opacity: 0.5 }, pressed: { opacity: 0.7 } });
