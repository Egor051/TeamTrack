import { Pressable, StyleSheet, View } from 'react-native';
import { ThemedText } from '@/components/ui/text';
import { colors, radii } from '@/components/ui/theme';
export function Checkbox({ checked, disabled, label, onPress }: { checked: boolean; disabled?: boolean; label: string; onPress: () => void }) { return <Pressable accessibilityRole="checkbox" accessibilityLabel={label} accessibilityState={{ checked, disabled }} disabled={disabled} onPress={onPress} hitSlop={6} style={({ pressed }) => [styles.touch, pressed && styles.pressed]}><View style={[styles.box, checked && styles.checked, disabled && styles.disabled]}>{checked ? <ThemedText style={styles.mark}>✓</ThemedText> : null}</View></Pressable>; }
const styles = StyleSheet.create({ touch: { width: 48, height: 48, alignItems: 'center', justifyContent: 'center' }, box: { width: 26, height: 26, borderRadius: radii.sm, borderWidth: 2, borderColor: colors.borderStrong, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.surface }, checked: { backgroundColor: colors.primary, borderColor: colors.primary }, mark: { color: colors.surface, fontWeight: '800', lineHeight: 20 }, disabled: { opacity: 0.5 }, pressed: { opacity: 0.7 } });
