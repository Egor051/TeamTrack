import { Pressable, StyleSheet, View } from 'react-native';
import { ThemedText } from '@/components/ui/text';
import { colors, radii } from '@/components/ui/theme';
export function SegmentedControl<T extends string>({ value, options, onChange, accessibilityLabel }: { value: T; options: { value: T; label: string }[]; onChange: (value: T) => void; accessibilityLabel: string }) { return <View accessibilityRole="tablist" accessibilityLabel={accessibilityLabel} style={styles.container}>{options.map((option) => { const selected = option.value === value; return <Pressable key={option.value} accessibilityRole="tab" accessibilityState={{ selected }} onPress={() => onChange(option.value)} style={[styles.option, selected && styles.selected]}><ThemedText type="small" style={[styles.text, selected && styles.selectedText]}>{option.label}</ThemedText></Pressable>; })}</View>; }
const styles = StyleSheet.create({ container: { flexDirection: 'row', alignSelf: 'flex-start', borderWidth: 1, borderColor: colors.border, borderRadius: radii.md, padding: 3, backgroundColor: colors.surfaceMuted }, option: { minHeight: 38, justifyContent: 'center', paddingHorizontal: 14, borderRadius: radii.sm }, selected: { backgroundColor: colors.surface }, text: { fontWeight: '600' }, selectedText: { color: colors.text } });
