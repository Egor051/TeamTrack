import { Pressable, StyleSheet, View } from 'react-native';
import { ThemedText } from '@/components/ui/text';
import { radii } from '@/components/ui/theme';
import { useTheme } from './theme-provider';
export function SegmentedControl<T extends string>({ value, options, onChange, accessibilityLabel }: { value: T; options: { value: T; label: string }[]; onChange: (value: T) => void; accessibilityLabel: string }) { const { colors: theme } = useTheme(); return <View accessibilityRole="tablist" accessibilityLabel={accessibilityLabel} style={[styles.container, { borderColor: theme.border, backgroundColor: theme.surfaceMuted }]}>{options.map((option) => { const selected = option.value === value; return <Pressable key={option.value} accessibilityRole="tab" accessibilityState={{ selected }} onPress={() => onChange(option.value)} style={[styles.option, selected && { backgroundColor: theme.surface }]}><ThemedText type="small" style={[styles.text, selected && { color: theme.text }]}>{option.label}</ThemedText></Pressable>; })}</View>; }
const styles = StyleSheet.create({ container: { flexDirection: 'row', alignSelf: 'flex-start', borderWidth: 1, borderRadius: radii.md, padding: 3 }, option: { minHeight: 38, justifyContent: 'center', paddingHorizontal: 14, borderRadius: radii.sm }, text: { fontWeight: '600' } });
