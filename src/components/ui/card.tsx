import { Pressable, StyleProp, StyleSheet, View, ViewStyle } from 'react-native';
import { colors, radii, shadows, spacing } from '@/components/ui/theme';
import { useTheme } from './theme-provider';
export function Card({ children, onPress, style, accessibilityLabel, muted }: { children: React.ReactNode; onPress?: () => void; style?: StyleProp<ViewStyle>; accessibilityLabel?: string; muted?: boolean }) { const { colors: theme } = useTheme(); const staticMuted = Boolean(muted && !onPress); const cardStyle = [styles.card, { backgroundColor: theme.surface, borderColor: theme.border }, muted && [styles.muted, { backgroundColor: theme.surfaceMuted }], staticMuted && [styles.staticMuted, { borderLeftColor: theme.primary }], style]; if (!onPress) return <View style={cardStyle}>{children}</View>; return <Pressable accessibilityRole="button" accessibilityLabel={accessibilityLabel} onPress={onPress} style={({ pressed }) => [cardStyle, pressed && styles.pressed]}>{children}</Pressable>; }
const styles = StyleSheet.create({ card: { backgroundColor: colors.surface, borderRadius: radii.md, borderWidth: 1, borderColor: colors.border, padding: spacing.lg, gap: spacing.sm, ...shadows.card }, muted: { backgroundColor: colors.surfaceMuted, shadowOpacity: 0 }, staticMuted: { borderRadius: radii.sm, borderLeftWidth: 3, shadowOpacity: 0, elevation: 0 }, pressed: { opacity: 0.78 } });
