import { Modal, Pressable, StyleSheet, View } from 'react-native';
import { Button } from '@/components/ui/button';
import { ThemedText } from '@/components/ui/text';
import { colors, radii, spacing } from '@/components/ui/theme';
export function ConfirmDialog({ visible, title, description, confirmLabel, busy, destructive = true, onConfirm, onCancel }: { visible: boolean; title: string; description: string; confirmLabel: string; busy?: boolean; destructive?: boolean; onConfirm: () => void; onCancel: () => void }) { return <Modal visible={visible} transparent animationType="fade" onRequestClose={onCancel}><Pressable style={styles.backdrop} onPress={busy ? undefined : onCancel} accessibilityRole="button" accessibilityLabel="Закрыть диалог"><Pressable style={styles.dialog} onPress={(event) => event.stopPropagation()} accessibilityViewIsModal><ThemedText type="h2">{title}</ThemedText><ThemedText style={styles.description}>{description}</ThemedText><View style={styles.actions}><Button variant="outline" disabled={busy} onPress={onCancel}>Отмена</Button><Button variant={destructive ? 'destructive' : 'primary'} loading={busy} onPress={onConfirm}>{confirmLabel}</Button></View></Pressable></Pressable></Modal>; }
const styles = StyleSheet.create({ backdrop: { flex: 1, backgroundColor: colors.overlay, padding: spacing.lg, justifyContent: 'center', alignItems: 'center' }, dialog: { width: '100%', maxWidth: 460, backgroundColor: colors.surface, borderRadius: radii.md, padding: spacing.xl, gap: spacing.lg }, description: { color: colors.textSecondary }, actions: { flexDirection: 'row', justifyContent: 'flex-end', flexWrap: 'wrap', gap: spacing.sm } });
