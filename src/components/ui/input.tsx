import { StyleSheet, TextInput, View, type TextInputProps } from 'react-native';
import { ThemedText } from '@/components/ui/text';
import { colors, radii, spacing } from '@/components/ui/theme';
export type InputProps = { placeholder?: string; value?: string; onChangeText: (text: string) => void; type?: 'text' | 'email' | 'password'; disabled?: boolean; error?: string; label?: string } & TextInputProps;
export function Input({ placeholder, value, onChangeText, type = 'text', disabled = false, error, label, ...rest }: InputProps) { return <View style={styles.container}>{label ? <ThemedText type="small" style={styles.label}>{label}</ThemedText> : null}<TextInput placeholder={placeholder} value={value ?? ''} onChangeText={onChangeText} autoCapitalize={rest.autoCapitalize ?? 'none'} keyboardType={type === 'email' ? 'email-address' : 'default'} secureTextEntry={type === 'password' || rest.secureTextEntry} editable={!disabled} placeholderTextColor={colors.textMuted} accessibilityLabel={rest.accessibilityLabel || label || placeholder} style={[styles.input, rest.multiline && styles.multiline, error && styles.inputError, disabled && styles.disabled]} {...rest} />{error ? <ThemedText type="caption" style={styles.error}>{error}</ThemedText> : null}</View>; }
const styles = StyleSheet.create({ container: { width: '100%', gap: spacing.sm }, label: { fontWeight: '600', color: colors.text }, input: { fontSize: 16, minHeight: 48, paddingHorizontal: 16, paddingVertical: 12, borderRadius: radii.md, borderWidth: 1, borderColor: colors.borderStrong, backgroundColor: colors.surface, color: colors.text }, multiline: { minHeight: 112, textAlignVertical: 'top' }, inputError: { borderColor: colors.error }, disabled: { backgroundColor: colors.surfaceMuted, color: colors.textMuted }, error: { color: colors.error } });
